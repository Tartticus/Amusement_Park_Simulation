name: "fantasyland"
version: "1.0.0"
config-version: 2
 
profile: "fantasyland"
 
model-paths:  ["models"]
test-paths:   ["tests"]
seed-paths:   ["seeds"]
macro-paths:  ["macros"]
 
target-path:  "target"
clean-targets: ["target", "dbt_packages"]
 
models:
  fantasyland:
 
    silver:
      +schema:       CORE
      +materialized: incremental
      +unique_key:   event_id
 
    gold:
      +schema:       GOLD
      +materialized: table